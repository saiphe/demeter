<%= form_for(@movie) do |f| %>
  <% if @movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% @movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "movie" %><br>
    <%= f.text_field :title %>
    <%= f.collection_select  :include_blank => true %>
  </div>
  <div>
  
  </div>
  <div class="actions">
    <%= image_submit_tag("check.jpeg", :class => 'add') %><%= link_to movies_path do %><%= image_tag 'back.jpeg', :class => 'back' %><% end %>
  </div>
<% end %>
